# AP5 – Eigen REST-AI experiment met curl

## Wat doet dit experiment?
- Stuurt een GET-request met curl naar een advies-API
- De API genereert een willekeurig advies in tekstvorm
- De JSON-output wordt in de terminal getoond

## Hoe uitvoeren?
In de terminal:

```bash
cd ~/devops-examen-2025/API/Ap5
./ap5_ai_curl.sh
## Screenshot
De screenshot van de output staat in:
API/Ap5/screenshots/ap5-ai-curl-output.png

Opslaan en sluiten.

---

### 5. Commit + push

```bash
cd ~/devops-examen-2025
git add API/Ap5
git commit -m "AP5 REST-AI curl experiment met advies-API toegevoegd"
git push
